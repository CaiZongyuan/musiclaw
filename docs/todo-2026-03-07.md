# TODO · 2026-03-07

本文档用于把当前仓库 **还未完成 / 未完全对齐原版 / 仍需验证** 的事项整理成一份可执行清单，方便下一位开发者直接按优先级推进。

原则：

- 以 `/root/Projects/Frontend/YesPlayMusic` Web 端为唯一产品基线
- 优先补 **原版已有功能与页面**，不优先扩展新能力
- 优先补 **主路径完整度**，再补视觉细节与边缘能力
- 完成标准不是“能打开”，而是“功能、结构、主交互尽量接近原版”

---

## P0 · 下一阶段最优先

当前说明：

- `Explore` 已在本轮完成主结构回迁：不再是占位页，已补分类浏览、更多分类面板、分类分页与播放器来源回跳
- 当前 P0 重点顺延为 `New Album` 与 `MV` 主路径

---

### 1. 收口 `New Album`

- `/new-album` 仍是占位页
- 首页虽然已能跳转，但目标页不完整

需要完成：

- 接入真实新专辑列表
- 补接近原版的筛选 / 排列 / 卡片密度
- 评估是否要恢复按区域/类型筛选
- 补从新专辑页播放 / 跳转专辑详情的完整链路

验收标准：

- 页面有真实列表，不再是说明性骨架
- 可直接进入专辑页
- 若页面提供播放入口，来源语义正确

---

### 2. 收口 `MV` 主路径

当前状态：

- `/mv/:id` 仍未完成真实播放页
- `/artist/:id/mv` 路由还未实现

需要完成：

- 实现 `/mv/:id` 的真实 MV 页面
- 实现 `/artist/:id/mv` 艺人 MV 列表页
- 从艺人页补“查看更多 MV”链路
- 校验 MV 封面、播放、标题、发布时间等信息结构

验收标准：

- 可以从艺人页进入 MV 列表，再进入单个 MV 页
- MV 页面不再是占位页
- 页面结构与原版主路径接近

---

## P1 · 核心页面剩余功能缺口

### 4. 继续扩 `Library`

当前已完成：

- liked songs 入口与列表
- 歌单
- 收藏专辑
- 收藏艺人
- 最近播放
- 用户名模式 / 账号模式分流

仍需完成：

- MV 分区
- 云盘分区
- 更完整的 tab / 筛选 / 过滤体验
- 更多原版上下文菜单与播放模式动作
- 更接近原版的分页、列表密度与按钮位置

重点注意：

- 用户名模式和账号模式能力不同，不能混用判断
- 账号态专属分区必须给出清晰降级提示

---

### 5. 继续扩 `Artist`

当前已完成：

- 头部信息区
- 最新发布
- 热门歌曲
- 专辑区块

仍需完成：

- MV 分区
- 相似艺人
- 更多原版的 section 节奏和“查看更多”行为
- 更完整的艺人信息细节

---

### 6. 继续扩 `Album`

当前已完成：

- 头部信息区
- 歌曲列表
- 基础播放链路

仍需完成：

- 更多附加信息区
- 同艺人更多专辑（`More by ...`）
- 多 Disc 分组展示
- 收藏 / 更多操作的完整原版行为

---

### 7. 继续扩 `Playlist`

当前已完成：

- 头部封面 + 信息区
- 歌曲列表
- 播放链路

仍需完成：

- 收藏歌单 / 更多操作
- 歌单内搜索
- 特殊歌单样式分支
- 更完整的大歌单加载 / 补全逻辑
- 更接近原版的按钮组与说明区节奏

---

## P2 · 播放器与全局体验剩余缺口

### 8. 播放器剩余 parity

当前已完成：

- 队列页
- 喜欢按钮
- 专辑 / 歌手跳转
- 来源语义基础链路
- 搜索来源恢复
- VIP 主链路修正

仍需完成：

- 歌词层最终 UI
- Explore / 首页更多播放入口的来源语义
- 更多旧版 hover / active / 文案节奏
- 更细粒度的列表上下文恢复

---

### 9. 继续收 `Navbar / 首页 / RoutePlaceholder` 剩余页

当前已完成：

- Navbar / Player / 首页已完成多轮视觉收口
- `Library / Playlist / Album / Artist` 已拆掉通用占位骨架

仍需完成：

- 继续处理仍带 `RoutePlaceholder` 痕迹的主路径页
- 把边缘页从“说明页”逐步拉回原版信息结构

重点目标页：

- `/explore`
- `/new-album`
- `/mv/:id`
- `/settings`

---

## P3 · 边缘能力与工程收尾

### 10. `Settings`

当前状态：

- 仍是说明页

需要完成：

- 主题
- 音质
- 歌词字号
- 缓存
- 代理相关设置
- 与现有 settings store 对接

---

### 11. `Last.fm Callback`

当前状态：

- 路由未实现

需要完成：

- 回调路由
- 基础参数处理
- 成功 / 失败反馈

---

### 12. 构建问题单独收口

当前状态：

- `bun run build` 仍未通过

已知阻塞：

- `src/routes/demo.i18n.tsx` 依赖的 `../logo.svg` 缺失
- 当前环境下 `wrangler` 日志写入权限问题

建议：

- 在主线页面 parity 继续推进前，不要优先花大量时间处理
- 如果单独开一轮“构建修复”，再集中收口

---

## 当前建议开发顺序

建议严格按以下顺序推进：

1. `Explore`
2. `New Album`
3. `MV` 主路径：`/mv/:id` + `/artist/:id/mv`
4. `Library` 剩余分区：MV / 云盘 / 更完整 tabs
5. `Artist`：MV / 相似艺人
6. `Album`：More by / 多 Disc / 更多附加信息
7. `Playlist`：搜索 / 收藏 / 特殊歌单样式
8. `Settings`
9. `Last.fm Callback`
10. 最后再单独处理 `build`

---

## 每一轮开发后的固定回归清单

每完成一轮，至少回归这些页面：

1. `/`
2. `/search?q=周杰伦&type=1&page=1`
3. `/playlist/<任意已知 id>`
4. `/album/<任意已知 id>`
5. `/artist/<任意已知 id>`
6. `/library`
7. `/library/liked-songs`
8. `/next`

如果本轮涉及发现页或 MV，再补：

9. `/explore`
10. `/new-album`
11. `/mv/<任意已知 id>`
12. `/artist/<任意已知 id>/mv`

---

## 当前不建议优先做的事

- 不要优先扩展 `about` 页面
- 不要优先扩展 `demo.*` 页面
- 不要把主线切回 `better-auth`
- 不要在 parity 未收口前继续做“新产品化”设计
- 不要把“能打开 / 能请求到数据”当成完成
