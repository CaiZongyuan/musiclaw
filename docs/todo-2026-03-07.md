# TODO · 2026-03-07

本文档用于把当前仓库 **还未完成 / 未完全对齐原版 / 仍需验证** 的事项整理成一份可执行清单，方便下一位开发者直接按优先级推进。

原则：

- 以 `/root/Projects/Frontend/YesPlayMusic` Web 端为唯一产品基线
- 优先补 **原版已有功能与页面**，不优先扩展新能力
- 优先补 **主路径完整度**，再补视觉细节与边缘能力
- 完成标准不是“能打开”，而是“功能、结构、主交互尽量接近原版”

---

## P0 · 下一阶段最优先

当前说明：

- `Explore`、`New Album`、`Settings` 都已经回到可用状态，不再作为本轮收尾 blocker
- 当前用户已明确：`MV` 与云盘功能本轮不做
- 当前这一轮里 `build`、`Last.fm Callback` 与文档同步都已完成
- 若继续开发，优先只剩可选的 parity 微调项

---

### 1. 本轮已完成项

- `build` 已修复：`src/logo.svg` 已补齐，`bun run build` 现已通过
- `WRANGLER_LOG_PATH` 已改为写入仓库内 `.wrangler/logs`
- `/lastfm/callback` 已实现最小可用回调链路
- README、中文 README、环境变量示例与进度文档已同步

---

## P1 · 核心页面剩余功能缺口

### 4. 继续扩 `Library`

当前已完成：

- liked songs 入口与列表
- 歌单
- 收藏专辑
- 收藏艺人
- 最近播放
- 用户名模式 / 账号模式分流

仍需完成：

- 更完整的 tab / 筛选 / 过滤体验
- 更多原版上下文菜单与播放模式动作
- 更接近原版的分页、列表密度与按钮位置

重点注意：

- 用户名模式和账号模式能力不同，不能混用判断
- 账号态专属分区必须给出清晰降级提示

---

### 5. 继续扩 `Artist`

当前已完成：

- 头部信息区
- 最新发布
- 热门歌曲
- 专辑区块

仍需完成：

- 相似艺人
- 更多原版的 section 节奏和“查看更多”行为
- 更完整的艺人信息细节

---

### 6. 继续扩 `Album`

当前已完成：

- 头部信息区
- 歌曲列表
- 基础播放链路

仍需完成：

- 更多附加信息区
- 同艺人更多专辑（`More by ...`）
- 多 Disc 分组展示
- 收藏 / 更多操作的完整原版行为

---

### 7. 继续扩 `Playlist`

当前已完成：

- 头部封面 + 信息区
- 歌曲列表
- 播放链路

仍需完成：

- 收藏歌单 / 更多操作
- 歌单内搜索
- 特殊歌单样式分支
- 更完整的大歌单加载 / 补全逻辑
- 更接近原版的按钮组与说明区节奏

---

## P2 · 播放器与全局体验剩余缺口

### 8. 播放器剩余 parity

当前已完成：

- 队列页
- 喜欢按钮
- 专辑 / 歌手跳转
- 来源语义基础链路
- 搜索来源恢复
- VIP 主链路修正

仍需完成：

- 歌词层最终 UI
- Explore / 首页更多播放入口的来源语义
- 更多旧版 hover / active / 文案节奏
- 更细粒度的列表上下文恢复

---

### 9. 继续收 `Navbar / 首页 / RoutePlaceholder` 剩余页

当前已完成：

- Navbar / Player / 首页已完成多轮视觉收口
- `Library / Playlist / Album / Artist` 已拆掉通用占位骨架

仍需完成：

- 继续处理仍带 `RoutePlaceholder` 痕迹的主路径页
- 把边缘页从“说明页”逐步拉回原版信息结构

重点目标页：

- `/explore`
- `/new-album`
- `/mv/:id`
- `/settings`

---

## P3 · 边缘能力与工程收尾

### 10. `Settings`

当前状态：

- 仍是说明页

需要完成：

- 主题
- 音质
- 歌词字号
- 缓存
- 代理相关设置
- 与现有 settings store 对接

---

### 11. `Last.fm Callback`

当前状态：

- 路由未实现

需要完成：

- 回调路由
- 基础参数处理
- 成功 / 失败反馈

---

### 12. 构建问题单独收口

当前状态：

- `bun run build` 仍未通过

已知阻塞：

- `src/routes/demo.i18n.tsx` 依赖的 `../logo.svg` 缺失
- 当前环境下 `wrangler` 日志写入权限问题

建议：

- 在主线页面 parity 继续推进前，不要优先花大量时间处理
- 如果单独开一轮“构建修复”，再集中收口

---

## 当前建议开发顺序

建议严格按以下顺序推进：

1. `build`
2. `Last.fm Callback`
3. 文档与 README 同步
4. 若继续追 parity，再回头微调 `Daily Songs / New Album / Settings`

---

## 每一轮开发后的固定回归清单

每完成一轮，至少回归这些页面：

1. `/`
2. `/search?q=周杰伦&type=1&page=1`
3. `/playlist/<任意已知 id>`
4. `/album/<任意已知 id>`
5. `/artist/<任意已知 id>`
6. `/library`
7. `/library/liked-songs`
8. `/next`

如果本轮涉及发现页或 MV，再补：

9. `/explore`
10. `/new-album`
11. `/mv/<任意已知 id>`
12. `/artist/<任意已知 id>/mv`

---

## 当前不建议优先做的事

- 不要优先扩展 `about` 页面
- 不要优先扩展 `demo.*` 页面
- 不要把主线切回 `better-auth`
- 不要在 parity 未收口前继续做“新产品化”设计
- 不要把“能打开 / 能请求到数据”当成完成
